# Handoffs

Create one handoff file per meaningful agent session using `TEMPLATE.md`.

Handoffs are distilled summaries, not raw chat logs. They should let another human or agent resume from repository state.
