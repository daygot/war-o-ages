# Audit Mobile Surface Availability

Status: Backlog
Owner: Unclaimed
Branch: docs/audit-mobile-surface
Created: 2026-06-12

## Goal
Resolve the mismatch between design docs referencing a mobile UI kit and the current bundle, which contains only `ui_kits/desktop/` and `ui_kits/shared/`.

## Acceptance Criteria
- Determine whether mobile artifacts exist elsewhere or are intentionally absent.
- Update `README.md`, `DESIGN_CONTEXT.md`, and `context/DESIGN.md` to match the actual repository state.
- If mobile is still desired, create a follow-up implementation task.
